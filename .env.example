# Required: target folder path to scan
TARGET_FOLDER=

# Optional: file glob pattern (default: *.py)
FILE_PATTERN=*.py

# Optional: language tag for code blocks (default: python)
CODE_BLOCK_TAG=python
